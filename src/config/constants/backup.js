// src/config/constants/backup.js

// ─── Backup ───────────────────────────────────────────────────────────────────
export const BACKUP_INTERVAL_MS = 24 * 60 * 60 * 1000; // 24 hours
export const MAX_BACKUPS_KEPT   = 7;                    // keep last 7 snapshots

// حد أقصى (بالبايت) لكل مستند/chunk في النسخة الاحتياطية. حد Firestore
// الفعلي لأي مستند هو ~1,048,576 بايت (1 ميجابايت) شامل كل الحقول —
// سايبين هامش أمان تحته (بدل ما نقرب من الحد بالظبط) عشان حقول زي
// counts/createdAt/index كمان بتاخد جزء بسيط من المساحة. لو حجم بيانات
// الشركة الكامل أكبر من ده، createBackup بيقسّمها على مستندات "chunks"
// فرعية متعددة بدل ما يحاول يكتبها كلها في مستند واحد.
export const BACKUP_CHUNK_BYTES = 900_000;

// حد أقصى منطقي (مليار جنيه) لأي حقل مالي/كمّي (سعر، تكلفة، مبلغ، راتب...).
// مش قيد على العمل الفعلي (ولا شركة هتوصل للرقم ده)، الهدف بس إنه يمسك
// أخطاء الكتابة (زي صفر زيادة بالغلط) ويمنع أي قيمة غير منطقية تدخل
// الحسابات وتفسد التقارير.
export const MAX_MONEY_VALUE = 1_000_000_000;
